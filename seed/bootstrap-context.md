---
kind: seed-note
scope: workstation
---

# Bootstrap context

This machine foundation provides configuration and workspace scaffolding. It does
not import transcripts, credentials, project databases, memory corpora, or RAG
indexes. Those are deliberate, project-specific operations performed after the
baseline has passed verification.
