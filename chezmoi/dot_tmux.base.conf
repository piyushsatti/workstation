# Tmux Base Configuration : shared across all machines
# Sourced by linux.conf or mac.conf

set -g status on
set -g status-position bottom
set -g default-terminal "tmux-256color"
set -g mouse on
set -g set-clipboard on
set -g focus-events on
set -g allow-passthrough on

# True color + OSC 52 clipboard fix for Alacritty
set -ga terminal-overrides ",xterm*:Tc:Ms=\\E]52;c;%p2%s\\007"

# Override snap-injected XDG_CACHE_HOME with the correct XDG default
set-environment -g XDG_CACHE_HOME "$HOME/.cache"

# Prefix key (sensible handles C-b unbind and send-prefix)
set -g prefix C-Space

# Reload config
bind r source-file ~/.tmux.conf \; display "Config reloaded"

# Vim style pane selection
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-option -g renumber-windows on
set-option -g allow-rename off
set-window-option -g automatic-rename off

# Use Alt-arrow keys without prefix key to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# Shift Alt vim keys to switch windows
bind -n M-H previous-window
bind -n M-L next-window

# Nord theme config (must be before plugin load) : status-left/right, window tabs
# come from the nordtheme/tmux plugin itself (src/nord-status-content.conf)
set -g status-right-length 200
set -g status-left-length 100

# Session persistence - auto save every 10 min, auto restore on start, auto boot via systemd
set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-processes 'vim nvim less man htop'
set -g @continuum-boot 'off'
set -g @continuum-restore 'on'
set -g @continuum-save-interval '10'

# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'nordtheme/tmux'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'fcsonline/tmux-thumbs'
set -g @thumbs-upcase-command 'xdg-open {}'
set -g @plugin 'omerxx/tmux-sessionx'
set -g @sessionx-bind 's'
set -g @plugin 'loichyan/tmux-toggle-popup'

run '~/.tmux/plugins/tpm/tpm'

# Nord's own status content uses ANSI "white" (nord5, #E5E9F0) for window
# titles and the clock : that's Nord's canonical terminal-white, but too
# bright against this dark a background. Override to nord4 (#D8DEE9),
# Nord's own default-foreground tone, after the plugin has already loaded.
set -g status-right "#($HOME/.tmux/plugins/tmux-continuum/scripts/continuum_save.sh)#{?client_prefix,#[fg=black]#[bg=brightcyan]#[fg=brightcyan]#[bg=black]#[nobold]#[noitalics]#[nounderscore]#[bg=brightcyan]#[fg=black]^SPACE,#[default]#[fg=default]#[bg=default]}#[default]#[fg=brightblack,bg=black,nobold,noitalics,nounderscore]#[fg=#D8DEE9,bg=brightblack] %Y-%m-%d #[fg=#D8DEE9,bg=brightblack,nobold,noitalics,nounderscore]#[fg=#D8DEE9,bg=brightblack] %H:%M #[fg=cyan,bg=brightblack,nobold,noitalics,nounderscore]#[fg=black,bg=cyan,bold] #H "
set -g window-status-format "#[fg=black,bg=brightblack,nobold,noitalics,nounderscore] #[fg=#D8DEE9,bg=brightblack]#I #[fg=#D8DEE9,bg=brightblack,nobold,noitalics,nounderscore] #[fg=#D8DEE9,bg=brightblack]#W #F #[fg=brightblack,bg=black,nobold,noitalics,nounderscore]"

# tmux defaults these three to "reverse", which swaps fg/bg on a window's
# status segment and breaks nordtheme/tmux's color-matched separator
# illusion (flat/blocky arrows instead of blended ones). Unfixed upstream:
# nordtheme/tmux#101, patch open as PR #102 since 2023, repo inactive.
set -g window-status-activity-style none
set -g window-status-bell-style none
set -g window-status-silence-style none

# Vi copy-mode (after TPM so it overrides Nord's mode-style)
set-window-option -g mode-keys vi
set -g mode-style "fg=#232731,bg=#EBCB8B"
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel
bind -n M-v copy-mode

# Mouse drag: copy to tmux buffer (server-wide, incl. popup) + OSC-52 to local clipboard, exit copy mode
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel

# Triple-click: select line, copy to clipboard, keep selection visible
bind-key -T root TripleClick1Pane select-pane -t= \; copy-mode -M \; send-keys -X select-line \; send-keys -X copy-pipe-no-clear
bind-key -T copy-mode-vi TripleClick1Pane select-pane \; send-keys -X select-line \; send-keys -X copy-pipe-no-clear

# Middle-click: paste from the shared tmux buffer; -p bracketed-paste prevents auto-execute
bind-key -T root MouseDown2Pane paste-buffer -p

# Escape exits copy mode (vi-style)
bind-key -T copy-mode-vi Escape send-keys -X cancel

# Auto-exit copy mode when scrolling hits the bottom
bind -T copy-mode-vi WheelDownPane select-pane \; send-keys -X -N 5 scroll-down \; if-shell -F "#{==:#{scroll_position},0}" "send-keys -X cancel"

# Splits and new windows inherit working directory
bind \\ split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"

# Fuzzy session switcher (prefix S)
bind S display-popup -E "tmux list-sessions -F '#{session_name}' | fzf --reverse | xargs tmux switch-client -t"

# Floating scratch terminal (Alt-g) - toggleable via tmux-toggle-popup
bind -n M-g run "#{@popup-toggle} -w80% -h80% -Ed'#{pane_current_path}'"

# Toggle pane sync for multi-server work (prefix a)
bind a setw synchronize-panes \; display "Sync #{?synchronize-panes,ON,OFF}"

# Reorder windows (Ctrl-Shift-Arrow)
bind -n C-S-Left swap-window -t -1 \; previous-window
bind -n C-S-Right swap-window -t +1 \; next-window
